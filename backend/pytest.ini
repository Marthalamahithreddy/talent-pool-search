# =============================================================
# FILE: backend/pytest.ini
# PURPOSE: pytest configuration.
#          Sets the test root and registers the "integration" marker
#          so real-API tests can be skipped in CI with -m "not integration".
# =============================================================

[pytest]
testpaths = tests
markers =
    integration: marks tests that call real external APIs (skip in CI)
